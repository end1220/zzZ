<UserControl x:Class="Float.Pages.Home"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignWidth="300" Height="300" Background="#FF444444">
	<Grid x:Name="LayoutRoot"  Margin="5,-13,5,5" ShowGridLines="True" Style="{StaticResource ContentRoot}" ScrollViewer.VerticalScrollBarVisibility="Hidden" Background="#FF747474">
		<ListBox Height="417" HorizontalAlignment="Left" Margin="-10,-10,-450,-110" Name="listBox1" VerticalAlignment="Top"
				Width="600" Background="Transparent" ItemsSource="{Binding}" ScrollViewer.CanContentScroll="True"
				ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Visible"  BorderBrush="White">
			<ListBox.ItemsPanel>
				<ItemsPanelTemplate>
					<WrapPanel ItemHeight="140" ItemWidth="120" >
					</WrapPanel>
				</ItemsPanelTemplate>
			</ListBox.ItemsPanel>
			<ListBox.ItemTemplate>
				<DataTemplate>
					<Grid HorizontalAlignment="Center" Background="#FFFF7D7D">
						<Grid.RowDefinitions>
							<RowDefinition Height="0"></RowDefinition>
							<RowDefinition Height="100" ></RowDefinition>
							<RowDefinition Height="40"></RowDefinition>
						</Grid.RowDefinitions>
						<Image Source="{Binding Image}" Grid.Row="1" Width="120" Height="100" HorizontalAlignment="Stretch"/>
						<TextBlock Text="{Binding Name}" Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="8pt" TextWrapping="Wrap" />
					</Grid>
				</DataTemplate>
			</ListBox.ItemTemplate>
		</ListBox>
	</Grid>
</UserControl>
